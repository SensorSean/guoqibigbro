# 国企大表哥 V1.3.3 使用手册

## 目录
1. [软件概述](#1-软件概述)
2. [使用场景](#2-使用场景)
3. [环境要求与安装](#3-环境要求与安装)
4. [界面导览](#4-界面导览)
5. [完整操作流程](#5-完整操作流程)
6. [按钮功能速查表](#6-按钮功能速查表)
7. [进阶功能](#7-进阶功能)
8. [常见问题](#8-常见问题)

---

## 1. 软件概述

**国企大表哥**是一款 Windows 桌面工具，用于将多个 Excel 数据源（.xlsx）自动归集、按列名和行标识匹配后填入一个空白目标模板。

| 特性 | 说明 |
|------|------|
| 运行方式 | 双击 `.exe` 即可，无需安装 Python |
| 离线运行 | 所有数据在本地处理，不联网、不上传 |
| 支持文件 | .xlsx / .xls / .csv |
| 多源归集 | 支持同时加载多个数据源，后加载优先级更高 |
| 智能匹配 | 自动识别源→目标字段映射（列名/同义词匹配） |
| 行级对齐 | 按「立项名称」等行标识自动对齐横排数据 |

---

## 2. 使用场景

| 场景 | 说明 |
|------|------|
| **建安项目台账归集** | 你有来自财务、工程、合同 3 个部门的数据源，需要合并填入统一的「项目台账」模板 |
| **多阶段数据整合** | 立项阶段、可研阶段、设计阶段的数据分散在不同 Excel 里，需一次性归集 |
| **报表数据自动填报** | 上级下发了一个空模板，你需要把分散在多个系统的数据填进去 |
| **数据清洗后批量回填** | 你已经用 Excel 整理好数据，但不想手工逐格粘贴 |

---

## 3. 环境要求与安装

### 3.1 系统要求
- Windows 10 / 11（64 位）
- 建议分辨率 ≥ 1366×768

### 3.2 必需组件
| 组件 | 说明 | 获取方式 |
|------|------|---------|
| **Microsoft Edge WebView2 Runtime** | 软件界面运行基础（约 120MB） | 大多数 Win11 系统已自带；Win10 用户如缺，运行 `安装WebView2.bat` |

> ⚠️ 如果没有 WebView2 Runtime，软件启动后会自动弹提示。运行 `安装WebView2.bat` 安装后重启软件即可。

### 3.3 可选组件
| 组件 | 说明 |
|------|------|
| Microsoft Excel 或 WPS | 用于浏览输出结果（也可以双击 `.xlsx` 用 WPS 打开） |

### 3.4 获取软件
从 `dist/国企大表哥_V1.3.3.exe`（37.7MB）复制到任意目录，双击运行。

---

## 4. 界面导览

软件有 **4 个页面**，通过顶部导航切换：

```
数据源  |  字段映射  |  执行填表  |  关于
```

### 4.1 数据源页
- **上传区**：点击「选择数据源文件」加载 .xlsx 文件（支持多选）
- **已加载标签**：每个文件显示为一个标签，含文件名、字段数、删除按钮
- **目标模板**：点击「选择目标模板」加载空白模板
- **表头区域配置**：为每个数据源指定表头所在行（支持合并表头 2~3 行）

### 4.2 字段映射页
- **智能匹配**：一键自动匹配源字段 → 目标字段
- **映射卡**：每张卡片显示一对源→目标映射，支持手动修改
- **行标识键**：指定用哪个字段（如「立项名称」）来对齐行
- **行映射列表**：展开查看每个项目的源→目标配对详情

### 4.3 执行填表页
- **输出路径**：自动预填「模板名_已填充_时间戳.xlsx」
- **执行按钮**：开始填充，进度条实时显示
- **结果区**：显示填充完成后的汇总信息，点击可打开结果文件

### 4.4 关于页
- 版本号、更新日志、使用说明

---

## 5. 完整操作流程

### 5.1 加载数据源
1. 切换到「数据源」页
2. 点击 **「选择数据源文件」**，在对话框中选择一个或多个 `.xlsx` 文件
3. 可多次点击追加（后加载的文件优先级更高）
4. 每个文件显示为一个标签，确认字段数量正确

### 5.2 设置表头区域（重要）
1. 如果数据源有**合并表头**（如第 1 行是类别、第 2 行是字段名），在下拉框中选择对应的行号（如「第 1-2 行」）
2. 表头行号必须与实际数据一致，否则字段匹配会失败
3. 点击右上角 **「刷新表头」** 确认修改生效

### 5.3 加载目标模板
1. 点击 **「选择目标模板」**
2. 选择你要填充的空白模板文件
3. 同样设置目标表头行号

### 5.4 字段匹配
1. 切换到「字段映射」页
2. 点击 **「智能匹配」**，软件自动为每条字段找到最佳源匹配
3. 检查匹配结果：**绿色**=自动匹配成功，**黄色**=建议确认，**灰色**=未匹配
4. 如需手动调整，点击未匹配字段的下拉框，手动选择源字段
5. 如需解除某个匹配，点击映射卡右侧的 ✕ 按钮

### 5.5 行标识键设置
1. 在「行标识键」区域查看默认选中的行键列（如「立项名称」）
2. 这是决定"哪一行是同一个项目"的关键字段
3. 如果默认不正确，点击下拉框手动选择

### 5.6 执行填表
1. 切换到「执行填表」页
2. 确认输出路径（默认保存到桌面）
3. 点击 **「开始执行」**
4. 等待进度条走到 100%
5. 执行完成后，结果文件会**自动打开**，也可以点击「打开结果文件夹」查看

---

## 6. 按钮功能速查表

### 导航栏
| 按钮 | 功能 |
|------|------|
| 数据源 / 字段映射 / 执行填表 / 关于 | 切换页面 |
| 🏠 图标（左上角） | 回到数据源页（头大表情） |
| 填表中（表情变化） | 执行时变为「填表中」，完成后变为「开心」 |

### 数据源页
| 按钮 | 功能 |
|------|------|
| **选择数据源文件** | 弹出文件对话框，选择一个或多个 .xlsx |
| **选择目标模板** | 选择空白模板文件 |
| 每个文件标签 × | 删除该数据源 |
| 表头行下拉框（数据源） | 选择该数据源的表头所在行 |
| 表头行下拉框（目标） | 选择目标模板的表头所在行 |
| 源→目标（箭头提示） | 表示数据流向，无操作功能 |

### 字段映射页
| 按钮 | 功能 |
|------|------|
| **智能匹配** | 自动匹配所有字段（列名 + 同义词） |
| **全部展开 / 折叠** | 展开或折叠所有映射卡 |
| 映射卡下拉框 | 手动选择源字段 |
| 映射卡 ✕ 按钮 | 解除该条映射 |
| 行标识键下拉框（源/目标） | 选择作为「项目身份」的列 |
| 行映射列表 > 展开 | 查看每个项目的具体配对 |

### 执行填表页
| 按钮 | 功能 |
|------|------|
| **开始执行** | 启动自动填表 |
| **取消** | 中止正在进行的填充 |
| **打开结果** | 用系统默认程序打开生成的文件 |
| **重置** | 重新选择输出路径，准备下一轮 |
| 输出路径输入框 | 点击可修改输出文件名/路径 |

### 关于页
| 元素 | 说明 |
|------|------|
| 版本卡片 | 显示当前版本号 |
| 更新日志 | 点击展开查看各版本改动 |
| 使用说明 | 4 步操作流程 + 小贴士 |

---

## 7. 进阶功能

### 7.1 多源优先级规则
后添加的数据源**优先级更高**。如果两个数据源都有同一字段，以后加载的为准。

### 7.2 手动行覆盖
在「行映射列表」中，可以手动修改某个目标行的源匹配对象（选择「无」即跳过不填）。

### 7.3 数据源去重
同一文件不会重复加载。如果误选了已加载的文件，软件会提示并跳过。

### 7.4 彩蛋功能
- 点击数据源页的小吉祥物图标
- 在搜索框中输入 `dabiaoge`
- 执行进度满格时有特效
- 连点「开始执行」10 次有惊喜

---

## 8. 常见问题

### Q1: 为什么字段识别不出来？
**A**: 检查表头行号是否正确。如果数据源有合并表头（第 1 行分类 + 第 2 行字段名），必须在下拉框选择「第 1-2 行」。

### Q2: 为什么字段匹配了，但填出来的全是空白？
**A**: 确认行标识键是否正确。建议使用「立项名称」这类逐行不同的字段，不要用「项目编号」（部分数据源可能没有编号列）。

### Q3: 为什么输出文件里数据"多了一倍"？
**A**: 目标模板可能有重复行（同一合同号出现多行）。V1.3.1 已修复此问题，一个源值最多填一个目标格。

### Q4: 双击 exe 没反应？
**A**: 检查两点：(1) 是否安装了 WebView2 Runtime；(2) 是否被杀毒软件拦截（本软件未签名，需选择「仍要运行」）。

### Q5: 生成的 .xlsx 文件能用 Excel 打开吗？
**A**: 完全可以。生成的文件是标准 .xlsx 格式，兼容 Microsoft Excel、WPS、LibreOffice。

### Q6: 数据源可以替换吗？
**A**: 先删除旧的数据源标签（点击 ×），再选择新文件加载即可。

---

> 本手册适用于国企大表哥 V1.3.3。如有更多问题，请联系开发团队。
> 出品：软件工坊 · 2026
