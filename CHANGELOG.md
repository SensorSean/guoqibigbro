# 更新日志

版本号约定：修订号 +1 = 缺陷修复，次版本号 +1 = 新增能力。

## V1.3.3（2026-08-30）

本次修复两处会**静默写错数据**的严重缺陷 —— 它们的共同特点是：程序不报错、
进度条正常走完、表格也填满了，但填进去的值是错的。为此同步补齐 40 项回归测试。

> ✅ 本版（V1.3.3）已发布带版本号修正的可执行文件，源码与二进制版本一致。

### 🐛 缺陷修复

**1. 行标识归一化误截断并列连词「之 / 及」，不同项目被当成同一个项目**

归一化规则 `[之及至到][^之及至到]*$` 本意是处理「XX路之B段」「A至B」这类区间写法，
但中文里「之 / 及」同时是高频**并列连词**，于是把连词的后半截整个删掉了：

| 原始项目名 | 修复前归一化 | 修复后归一化 |
|---|---|---|
| 示范城市之光医院门诊综合楼二期项目 | `示范城市` | `示范城市之光医院门诊综合楼二期项目` |
| 示范城市之光商业综合体项目 | `示范城市` | `示范城市之光商业综合体项目` |
| 示范城市之光写字楼三期项目 | `示范城市` | `示范城市之光写字楼三期项目` |
| 示例学校 | `示例学校` | `示例学校` |
| 示例学校及周边道路填土夯实工程 | `示例学校` | `示例学校及周边道路填土夯实工程` |

不同项目塌陷成同一个键后，行匹配得分恒为 1.0（被判定为「精确匹配」），
1:1 贪心对齐无法区分，于是把 A 行的数据填进了 B 行。

修复方式：**收窄规则，而不是删除规则**。
- `至 / 到` 是真正的区间连接词（「A至B」），保留无条件截断；
- `之 / 及` **仅当**尾部是区段标记（`段` / `区间` / `幅`）时才截断，例如「A路之B段」→「A路」。

> ⚠️ 维护提醒：直接删掉那条 `re.sub` 会**静默**破坏 V1.2.5 引入的区间归一能力 ——
> `_ROAD_RE` 只解析**带括号**的写法（「某路（A路-B路）」），接管不了无括号的「A路之B段」。

**2. 同义词词典把「项目负责人」与「项目经理」并成一组，整列填错人**

`core/matcher.py` 内置的 `STRICT_SYNONYMS` 与 `同义词词典.json` 中，
`负责人 / 项目负责人 / 项目经理 / 项目责任人 / 法人代表` 曾被归为同一同义词组。
真实台账里这通常是**两列、两个不同的人**，于是目标列「项目负责人」在没有同名源列时，
被同义命中源列「项目经理」（得分 95.0），整列填成了错的人。

修复：拆为两组互不隶属的同义词 ——
- `负责人 / 项目负责人 / 项目责任人`
- `项目经理 / 项目经理人 / 项目经手人`

`法人代表` 直接移除（它本就有独立分组）。修复后匹配得分由 95.0 降为 0.0。

> ⚠️ 维护提醒：同义词有**两处来源** —— 写死在 `core/matcher.py` 的 `STRICT_SYNONYMS`
> 与外部的 `同义词词典.json`。**只改 JSON 一处修不好这个 bug**，两处都必须拆。

### ✅ 新增回归测试

| 测试文件 | 项数 | 覆盖内容 |
|---|---|---|
| `tests/test_rowkey_normalize.py` | 22 | 并列连词不得误截断；区段「之X段」与区间「A至B」的收敛能力不回潮 |
| `tests/test_synonym_split.py` | 18 | 「项目负责人」与「项目经理」必须互斥 |

```bash
pip install pytest
python -m pytest tests -q   # 期望 40 passed
```

> 注：本仓库不含 `dist/`，其中 1 项校验「`dist/同义词词典.json` 与源词典是否一致」的用例会
> 自动跳过，故在本仓库运行结果为 `39 passed, 1 skipped`，属正常。

这 40 项测试**不是摆设**：把代码回退到修复前，其中 18 项会立即失败。

### 📊 验证结果

在 131 条真实项目名（源键顺序反转）上做差分对比：错配 **26 → 22** 条，
修好 4 条且**零回归**；另一组 400 行 / 86 条对齐的样本，新旧版本结果**完全一致**，确认无副作用。

### ⚠️ 已知问题（本版未修）

括号内的内容会被整体剥离，导致不同项目塌陷成同一个键，
例如 `示范路` 与 `示范路（A路-B路）` 均归一化为 `示范路`；
同理，写在括号里的期次信息（`(一期)` / `(二期)`）也会被吞掉。
上述 131 条样本中剩余 22 条错配即由此引起，将在后续版本处理。

## V1.3.1

首个开源版本（Apache-2.0）。多源 Excel 归集、字段智能匹配、行标识对齐、保留格式写回。
